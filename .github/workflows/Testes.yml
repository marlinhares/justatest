name: Action-Testes
on:
  push:
    branches: [ master ]
  workflow_dispatch:
jobs:
  testJob:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger jenkins job
        uses: joshlk/jenkins-githubaction@master
        with:
          url: "https://esteira.dev.processoeletronico.gov.br"
          job_name: "PEN/jobtmp2"
          username: "seges"
          api_token: "115335ca72df6b9ce80a98853693e9a77b"
          #parameters: '{"database": "sqlserver", "urlGitSpe": "value1"}'
          timeout: "40"
          start_timeout: "10"
      - name: Get job status
        run: echo "Job status is ${{ steps.job-build.outputs.build_url }}"
