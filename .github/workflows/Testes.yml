name: Action-Testes
on:
  push:
    branches: [ master ]
  workflow_dispatch:
jobs:
  testJob:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger jenkins job
        uses: jabbukka/jenkins-trigger@main
        with:
          url: "https://esteira.dev.processoeletronico.gov.br"
          job_name: "PEN/job/jobtmp2"
          user_name: "seges"
          api_token: "115335ca72df6b9ce80a98853693e9a77b"
          parameter: '{"database": "sqlserver", "urlGitSpe": "value1"}'
          wait: "true"
          timeout: "1000"
